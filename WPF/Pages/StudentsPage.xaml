<Page x:Class="WPF.Pages.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WPF.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="700"
      Title="StudentsPage"
      DataContext="{Binding Source={StaticResource Locator}, Path=Students}">

    <Page.Resources>
        <ResourceDictionary Source="/src/styles/Placeholder.xaml" />
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Width="205">
            <Label Content="Etudiants" Margin="5" FontFamily="Segoe Script"
                   Foreground="#FF177CC1" FontSize="16" FontWeight="Bold" />
            <Button Content="Ajouter" Margin="5" Padding="5" HorizontalAlignment="Stretch"
                    Command="{Binding AddStudentCommand}"
                    CommandParameter="{Binding SelectedItem, ElementName=StudentsDatagrid}" />
            <Button Content="Modifier" Margin="5" Padding="5" HorizontalAlignment="Stretch"
                    Command="{Binding }"
                    CommandParameter="{Binding SelectedItem, ElementName=StudentsDatagrid}" />
            <Button Content="Supprimer" Margin="5" Padding="5" HorizontalAlignment="Stretch"
                    Command="{Binding RemoveStudentCommand}"
                    CommandParameter="{Binding SelectedItem, ElementName=StudentsDatagrid}" />
            <Label Content=" " />
            <Label Content="Chercher" Margin="5" />
            <TextBox Style="{StaticResource placeholder}" Tag="Write here..."
                 HorizontalAlignment="Stretch" VerticalAlignment="Top" Margin="5" />
        </StackPanel>

        <Button  Grid.Column="0" Content="Retour" Margin="5" VerticalAlignment="Bottom"
                 Command="{Binding ChangeToMainPageCommand}" />

        <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                      Width="2" IsEnabled="False" />

        <DataGrid x:Name="StudentsDatagrid" ItemsSource="{Binding Collection, Mode=TwoWay}"
                  Grid.Column="2" Margin="5" IsReadOnly="True" />

        <!--<DataGrid Grid.Column="2" Margin="5" ItemsSource="{Binding Collection}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Age" Binding="{Binding Age}" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
            </DataGrid.Columns>
        </DataGrid>-->
    </Grid>
</Page>