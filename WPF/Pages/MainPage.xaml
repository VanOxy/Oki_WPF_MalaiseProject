<Page x:Class="WPF.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:WPF.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="700"
      Title="MainPage"
      DataContext="{Binding Source={StaticResource Locator}, Path=Main}">

    <Grid>
        <StackPanel>
            <Label Content="{Binding MyString}" Height="30" />
            <Button Content="Increment Counter" Command="{Binding IncrementCounterCommand}" />
            <Label Height="30" Content="Juste a blanc space label" />
            <Button Content="Get info from Service" Command="{Binding AskWebServiceCommand}" />
            <TextBox Text="{Binding ServiceAsk}" />
            <Label Content="{Binding ServiceAnswer}" Height="25" />
            <Label Height="50" />
        </StackPanel>
        <Button Content="Go to SeconPage" Command="{Binding ChangeToSecondPageCommand}" VerticalAlignment="Bottom" />
    </Grid>
</Page>